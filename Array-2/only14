// Given an array of ints, return true if every element is a 1 or a 4.
public boolean only14(int[] nums) 
{
  boolean oneOrFour = true;
  
  int i = 0;
  while (i < nums.length && oneOrFour)
  {
    oneOrFour = oneOrFour && ((nums[i] == 1) || (nums[i] == 4));
    i++;
  }
  return oneOrFour;
}
