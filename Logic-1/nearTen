// Given a non-negative number "num", return true if num is within 2 
// of a multiple of 10. Note: (a % b) is the remainder of dividing a by b, so (7 % 5) is 2.
public boolean nearTen(int num) 
{
  assert num > 0;
  if (num % 10 <= 2 || Math.abs(num % 10 - 10) <= 2)
    return true;
  else
    return false;
}
