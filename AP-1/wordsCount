// Given an array of strings, return the count of the number of strings with the given length.
public int wordsCount(String[] words, int len) 
{
  int count = 0;
  int alen = words.length;
  for (int i = 0; i < alen; i++)
  {
    if (words[i].length() == len)
      count++;
  } // end for
  return count;
}
